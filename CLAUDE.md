# CLAUDE.md — 나스닥 100 MACD 스크리너

이 파일은 클로드 코드가 이 저장소에서 작업할 때 항상 따르는 원칙이다.

## 프로젝트 한 줄 요약

나스닥 100 종목의 일봉을 매일 받아 MACD 중심 1:2:6 분할 전략 신호를 판정하고, 텔레그램 일일 브리핑으로 보내는 스크리너. 자동매매가 아니라 사용자가 브리핑을 보고 직접 주문하는 수동 매매용이다.

## 전략의 기준 문서

- 모든 매매 규칙은 `docs/strategy_v3.md`(MACD 중심 통합 매매 전략 v3)를 따른다.
- 문서에 없는 규칙을 임의로 만들지 않는다. 애매하면 구현을 멈추고 질문 목록으로 남긴다.
- 문서의 [가정] 값은 코드에 하드코딩하지 않고 `config.yaml`에서 읽는다.

## 단계 작업 원칙

- 작업은 단계(P1~P5) 단위로 진행하고, 지시받은 단계의 범위만 구현한다.
- 다음 단계 기능(신호, 상태 전이, 텔레그램 등)을 미리 만들지 않는다.
- 단계가 끝나면 마지막에 "완료 보고" 형식으로 결과를 출력한다 (각 단계 지시문에 형식이 있음).

## 구조 원칙

```
config.yaml   설정·가정값
data/         입출력 전용 (시세, 구성 종목, 실적 일정)
core/         순수 함수만 (네트워크·파일·DB·현재 시각 접근 금지)
engine/       daily.py(오늘 실행), backtest.py(과거 재생) — 둘 다 같은 core 사용
notify/       브리핑 문자열, 텔레그램 발송
store/        SQLite, 구글 드라이브 연동
scripts/      일회성 점검·리포트 스크립트
tests/        pytest
docs/         전략 문서
```

- `core/`의 함수는 DataFrame과 설정값을 받아 결과만 반환한다. 같은 입력이면 항상 같은 출력이어야 한다.
- 라이브 실행과 백테스트는 반드시 같은 `core/` 함수를 쓴다. 백테스트 전용 지표 코드를 따로 만들지 않는다.

## 데이터·지표 규칙 (위반 금지)

1. **미래 데이터 금지**: `shift(-n)` 같은 음수 이동을 쓰지 않는다. 일목 구름은 과거 값을 오늘로 끌어와(`shift(D)`) 비교한다.
2. **확정 봉만 사용**: 미국장이 열려 있는 동안의 당일 봉은 버린다.
3. **가격 기준**: 분할 반영 가격(yfinance `auto_adjust=False`의 `Close`)을 쓴다. 배당 수정 가격(`Adj Close`)은 쓰지 않는다 — 트레이딩뷰 기본 차트와 맞추기 위함.
4. **RSI**: Wilder 평활 (`ewm(alpha=1/14, adjust=False)`).
5. **MACD**: 12, 26, 9, `ewm(span=..., adjust=False)`.
6. **일목 이동 칸수 D**: `config.yaml`의 값 사용 (기본 26, 트레이딩뷰 대조 후 25로 바뀔 수 있음).
7. 종목당 최소 300거래일을 받는다.

## 코딩 스타일

- Python 3.11, pandas, numpy, yfinance, pyyaml, pytest.
- 식별자·코드는 영어, 주석·문서·출력 메시지는 한국어.
- 함수마다 입력·출력 형태를 적은 짧은 docstring을 단다.
- 네트워크 실패는 조용히 넘기지 않는다. 실패 종목 목록을 모아 보고한다.

## 보안

- 텔레그램 토큰, chat_id, 구글 인증 파일은 환경변수나 `.env`로만 읽고, 절대 커밋하지 않는다.
- `.gitignore`에 `.env`, `*.db`, `data/cache/`, `outputs/`, 인증 JSON을 포함한다.
- `.env`나 인증 정보의 **값**은 어떤 경우에도 출력·repr·로그하지 않는다. 존재 여부와 길이만 확인한다 (P3.2 0번).

## 테스트 원칙

- 새 지표·신호 함수는 테스트와 함께 만든다.
- 미래 데이터 방지 테스트(데이터를 t일까지 자른 결과와 전체 데이터의 t일 값이 같은지)는 모든 지표에 적용한다.
- 테스트는 네트워크 없이 돌아가야 한다 (고정 CSV나 합성 데이터 사용).
